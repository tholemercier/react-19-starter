frozen-lockfile=true
# blocks subdependencies that use exotic specifiers like git: or remote tarball URLs. 
# Attackers use these to pull code from arbitrary sources, bypassing the registry entirely.
block-exotic-subdeps=true
# Helps delaying potential supply chain attacks (3 days)
min-release-age = 3